{
  "name": "@blerp/shared",
  "version": "1.0.0",
  "private": true,
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "generate": "openapi-typescript ../../openapi/blerp.v1.yaml -o src/schema.ts && bun -e 'const f=\"src/schema.ts\";let t=await Bun.file(f).text();t=t.replace(/T extends any\\[\\]/g,\"T extends unknown[]\");await Bun.write(f,t)'",
    "lint": "eslint ."
  },
  "dependencies": {
    "openapi-fetch": "^0.9.3"
  },
  "devDependencies": {
    "@blerp/config": "workspace:*",
    "openapi-typescript": "^6.7.5",
    "typescript": "^5.4.5"
  },
  "type": "module"
}
